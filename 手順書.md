# 動画テロップ自動生成ツール 手順書

---

## ▼ 最初の1回だけ：セットアップ

ターミナルを開いて、以下を1行ずつ実行してください。

```
brew install ffmpeg
```

```
pip3 install openai-whisper
```

---

## ▼ 毎回の作業手順

### STEP 1｜動画ファイルを用意する
編集ソフトから書き出した `.mp4` ファイルをどこかに置いておく。

---

### STEP 2｜テロップを生成する
ターミナルで以下を実行する（ファイルパスは自分のものに変更）:

```
python3 ~/Desktop/字幕制作ツール/telop.py 動画ファイルのパス.mp4
```

#### ファイルパスを簡単に入力する方法
`python3 ~/Desktop/字幕制作ツール/telop.py ` まで打ったあと、
Finderから動画ファイルをターミナルにドラッグ＆ドロップするとパスが自動入力されます。

#### 実行中の表示
```
今日の撮影.mp4 → フルテロップにします

  音声を抽出中...
  音声を聞き取り中...   ← 数分かかります。待つだけでOK
  完了 ✓
  テロップ用に整形中...

完了 ✓ → 今日の撮影.srt
```

動画と同じフォルダに `.srt` ファイルができたら成功です。

---

### STEP 3｜PowerDirector に読み込む

1. PowerDirector でプロジェクトを開く
2. 左メニューの **「T」アイコン（字幕ルーム）** をクリック
3. **「字幕ファイルのインポート」** をクリック
4. できあがった `.srt` ファイルを選択
5. タイムラインに字幕が自動で並ぶ → タイミングを確認して完成

---

## ▼ うまくいかないときは

| 症状 | 対処法 |
|------|--------|
| `command not found: python3` | `python3 --version` で確認。入っていなければ brew install python |
| `command not found: whisper` | `pip3 install openai-whisper` を再実行 |
| 文字起こしの精度が低い | telop.py 内の `small` を `medium` または `large` に変更 |
| エラーメッセージが出た | エラー文をそのまま Claude Code に貼り付けて「直して」と頼む |

# 動画テロップ自動生成ツール (telop.py)

動画ファイルを渡すだけで、フィラー（「えー」「あの」など）を削除した字幕ファイル（.srt）を自動生成します。PowerDirector にそのまま読み込めます。

## セットアップ（初回のみ）

```bash
# ffmpeg のインストール
# Mac:
brew install ffmpeg
# Windows: https://ffmpeg.org/download.html からダウンロード
#（または winget install ffmpeg）

# Whisper のインストール
pip install openai-whisper
```

## 使い方

```bash
python telop.py 動画ファイル.mp4
```

動画と同じフォルダに `動画ファイル.srt` が生成されます。

### 実行イメージ

```
今日の撮影.mp4 → フルテロップにします

  音声を抽出中... → 今日の撮影.wav
  音声を聞き取り中...
  完了 ✓ (42セグメント検出)
  テロップ用に整形中（17文字前後）...

完了 ✓ → 今日の撮影.srt
```

## PowerDirector への読み込み

1. PowerDirector でプロジェクトを開く
2. **字幕ルーム**（左メニューの「T」アイコン）を開く
3. **字幕ファイルのインポート** ボタンをクリック
4. 生成された `.srt` ファイルを選択
5. タイムラインに字幕が自動配置されるので、タイミングをサッと確認して完成

フォントや色は字幕ルーム内でまとめて変更できます。

## カスタマイズ

`telop.py` の上部にある設定で調整できます。

| 設定 | 内容 | デフォルト |
|------|------|-----------|
| `FILLERS` | 削除するフィラーのリスト | えー、あの、まあ、なんか など |
| `MAX_CHARS` | テロップ1行の最大文字数 | 17文字 |

### 精度を上げたいとき

`telop.py` 内の `--model small` を変更します。

| モデル | 速度 | 精度 |
|--------|------|------|
| `small` | 速い | 普通（デフォルト） |
| `medium` | やや遅い | 高い |
| `large` | 遅い | 最高 |

GPUがあるPCなら `large` でも実用的な速度で動きます。

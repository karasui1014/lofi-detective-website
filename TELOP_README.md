# 動画テロップ自動生成ツール (telop.py) — やり方ガイド

動画ファイルを渡すだけで、フィラー（「えー」「あの」など）を自動削除した字幕ファイル（.srt）を作ります。
できた字幕は PowerDirector にそのまま読み込めます。

```
動画.mp4 → [telop.py] → 動画.srt → PowerDirectorに読み込み → 完成
```

---

## 準備（最初の1回だけ）

ターミナル（Macなら「ターミナル.app」、Windowsなら「PowerShell」）を開いて、以下を1行ずつコピペして実行します。

### Mac の場合

```bash
# ① Homebrew が入っていなければインストール（5分くらい）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# ② ffmpeg（動画から音声を取り出すツール）
brew install ffmpeg

# ③ Whisper（音声を文字にするAI）
pip3 install openai-whisper
```

### Windows の場合

```powershell
# ① ffmpeg
winget install ffmpeg

# ② Whisper
pip install openai-whisper
```

> うまくいかないときは、エラーメッセージをそのままClaude Codeに貼り付けて「これ直して」と言えばOKです。

---

## 毎回の使い方（3ステップ）

### ステップ1: 動画をフォルダに置く

編集ソフトから書き出した動画ファイル（.mp4）を、いつものフォルダに入れる。準備はこれで終わり。

### ステップ2: スクリプトを実行する

ターミナルで以下を実行（ファイル名は自分の動画に置き換える）:

```bash
python3 telop.py ~/Movies/今日の撮影.mp4
```

> Claude Code を使っているなら、「`~/Movies/今日の撮影.mp4` をテロップにして」と日本語で頼むだけでもOK。

実行すると、こんな表示が出ます:

```
今日の撮影.mp4 → フルテロップにします

  音声を抽出中... → 今日の撮影.wav
  音声を聞き取り中...        ← ここが一番時間かかる（10分の動画で数分）
  完了 ✓ (42セグメント検出)
  テロップ用に整形中（17文字前後）...

完了 ✓ → 今日の撮影.srt
```

動画と同じフォルダに `今日の撮影.srt` ができています。

### ステップ3: PowerDirector に読み込む

1. PowerDirector でプロジェクトを開く
2. 左メニューの **「T」アイコン（字幕ルーム）** をクリック
3. **「字幕ファイルのインポート」** ボタン（フォルダのアイコン）をクリック
4. さっきできた `.srt` ファイルを選択
5. タイムラインに字幕が自動で並ぶので、タイミングをサッと確認して完成

フォントや色は字幕ルーム内でまとめて変更できます。

---

## よくある質問

**Q. 時間はどのくらいかかる？**
10分の動画で、普通のPCなら3〜10分くらい。GPU搭載PCなら1〜2分。

**Q. 精度が物足りない**
`telop.py` 内の `--model small` を `medium` か `large` に書き換えると精度が上がります（その分遅くなります）。

| モデル | 速度 | 精度 |
|--------|------|------|
| `small` | 速い | 普通（デフォルト） |
| `medium` | やや遅い | 高い |
| `large` | 遅い | 最高 |

**Q. 消したいフィラーを追加したい**
`telop.py` 上部の `FILLERS` リストに追加するだけ。Claude Codeに「『ていうか』もフィラーに追加して」と頼んでもOK。

**Q. 1行の文字数を変えたい**
`telop.py` 上部の `MAX_CHARS = 17` の数字を変えるだけ。

**Q. エラーが出た**
エラーメッセージをそのままClaude Codeに貼り付けてください。
